package Hashmaps;

import java.util.*;

public class longestConsecutiveSequence {
    
    public static ArrayList<Integer> findLargestSequence(int arr[]){

        HashMap<Integer,Boolean> map = new HashMap<>();

        for(int i=0;i<arr.length;i++){
            map.put(arr[i], true);
        }

        for(int i=0;i<arr.length;i++){
            map.get()
        }
    }
}
